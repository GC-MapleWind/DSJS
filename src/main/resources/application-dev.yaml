spring:
  datasource:
    url: jdbc:oracle:thin:@dsjs_high?TNS_ADMIN=./src/main/resources/OracleCloud
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    generate-ddl: true
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.OracleDialect
        jdbc:
          time_zone: Asia/Seoul

api-key: ${API_KEY}
